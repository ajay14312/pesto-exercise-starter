const argsString = (message, args) => {
  const splitTheMessage = message.split('{}');
  const messageObject = {};
  for(let key of splitTheMessage) {
    //messageObject
  }
};

export { argsString };
